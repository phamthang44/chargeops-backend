# Vietnamese message bundle (UTF-8).
#
# Error copy moved to the frontend (localized by error `code`). Add keys here
# only for backend-rendered messages (e.g. email templates).
invalid.request.format="Lỗi format request"