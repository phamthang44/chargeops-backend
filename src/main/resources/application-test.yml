spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:chargeops_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: none
