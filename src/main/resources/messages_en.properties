# Backend message bundle.
#
# Error/user-facing copy is intentionally NOT here: the API returns a stable
# error `code` and the frontend owns localization (see BaseErrorCode).
#
# Add keys here only for messages the backend itself must render (e.g. email
# templates), then provide locale variants in messages_vi / messages_en.
